[pytest]
markers =
    unit: unit tests without DB/network
    api: FastAPI endpoint tests with mocked services
    integration: tests that require TEST_DATABASE_URL and PostgreSQL pgvector
    scenario: user scenario tests with mocked LLM
